# core: protocol + crypto + state machine. Pure Rust, no I/O, no platform deps.
# Hardened: `#![forbid(unsafe_code)]` at lib.rs (Principle 2).
#
# Eventual deps (added per milestone):
#   M1: rustls (TLS 1.3), ed25519-dalek (device certs), x509-cert (cert gen).
#   M3: nothing new — WoL packet construction is a few bytes, no crypto.

[package]
name         = "wake-my-pc-core"
version      .workspace = true
edition      .workspace = true
rust-version .workspace = true
license      .workspace = true
repository   .workspace = true

[lints]
workspace = true

[dependencies]
# (none yet — M0 stub)

[dev-dependencies]
# (none yet — KAT vectors come in M1)
